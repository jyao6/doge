<% provide(:title, "Remove photos") %>
<p>
<% @service.photos.each do |p| %>
<%= link_to "delete", service_photo_path(@service.id, p.id), method: :delete,
                                  data: { confirm: "You sure?" } %>
 <%= image_tag p.img(:thumb)  %>
  <p>
<% end %>